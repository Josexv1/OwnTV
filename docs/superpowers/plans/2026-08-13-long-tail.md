# Phase-5 Long Tail Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the audit's remaining inventory — Search/EPG/Downloads/HomeGuideSlice/TopBar accent sites, the app-wide PRIMARY-at-rest enforcement, the phase-4 review follow-ups, and the orphaned-strings prune — completing the design-compliance roadmap.

**Architecture:** Targeted color-contract edits per feature file, one file-private extraction (Search `ResultRow`), a judgment sweep over `OwnTVButton` call sites, and a deletions-only resource prune. No new public components.

**Tech Stack:** Kotlin, Jetpack Compose for TV (tv-material3), existing `ui/components/` (`FocusableSurface`, `OwnTVButton`).

## Global Constraints

- **Behavior unchanged:** search/EPG/downloads logic, focus order, chip actions, player timings. Composition/visual only — except the sanctioned PRIMARY→SECONDARY demotions (visual by design).
- **CTA ruling (user-approved, binding):** PRIMARY-at-rest marks exactly ONE default action per visually-contiguous action group; extras demote to `OwnTVButtonStyle.SECONDARY`.
- **i18n:** no new strings, no string edits. Task 6 is deletions-only, mirrored across every `values-*` locale file. `PluralsCandidate` fatal.
- **Git hygiene:** stage edited files by explicit path only; NEVER `git commit -am`/`git add -A` (user's uncommitted gradle files in the tree).
- **Color/keep contract (exhaustive):** spec table. KEPT: Search favorite icon (SearchScreen.kt:448), EPG catchup glyph (GuideCore.kt:135) + red now-line (`colors.favorite`) + progress fills, Downloads progress fills (:234/:304), HomeGuideSlice progress fill (:543), TopBar static SectionChip tint, all phase-1..4 sanctioned accents.
- **Gates per task:** `./gradlew :app:compileStandardDebugKotlin lintStandardDebug` (0 errors); `git status --porcelain -- app/src/main/res` empty for Tasks 1-5 (Task 6 touches res by design, deletions only).
- Line numbers verified live on `main` @ aa077d8 — still verify surrounding context before editing.
- Branch: `long-tail` off `main`.

---

### Task 1: Search — shared ResultRow + color edits

**Files:**
- Modify: `app/src/main/java/tv/own/owntv/features/search/SearchScreen.kt` (:252-290 ResultRow triplication; :362 subtitle; :378-388 jump-to chips; :439 title recolor; ad-hoc fontWeights :362/:389/:490)

- [ ] **Step 1 (commit A: extraction).** Read the three `SearchItem` result-row cases (:252-290 region) in full. Extract the shared row skeleton into ONE file-private composable, e.g. `@Composable private fun ResultRow(title: String, subtitle: String?, icon: OwnTVIcon, favorite: Boolean, focused-state plumbing per the existing FocusableSurface usage)` — lift the EXACT layout of the most complete case verbatim and parameterize only what actually differs between the three cases (icon, subtitle text, favorite flag, onClick). Before consolidating, diff the three: if any case carries extra content the shared row can't express (e.g. a live badge), keep that piece as a trailing slot or leave that case local and extract the other two — record the choice. Route all three cases through it. Commit: `git add app/src/main/java/tv/own/owntv/features/search/SearchScreen.kt && git commit -m "Search: single ResultRow for all result kinds"`
- [ ] **Step 2 (commit B: colors).**
  - `:362` (now shifted): subtitle `color = colors.primary, fontWeight = FontWeight.SemiBold` → `color = colors.onSurfaceVariant`, drop the `fontWeight` override (labelLarge already carries its weight). Same treatment for the other ad-hoc `fontWeight`s at the audit's :389/:490 sites IF they sit on token styles that already encode weight — verify each in situ; leave any that adjust a style with no weight of its own, and record.
  - Jump-to chips (:378-388): `background(if (focused) colors.primary else colors.primaryContainer)` → `background(colors.surfaceContainerHigh)`; icon tint and text `if (focused) onPrimary else onPrimaryContainer` → `colors.onSurface` (both states). Ring stays the focus signal.
  - ResultRow title (:439, now inside the shared composable): `color = if (focused) colors.primary else colors.onSurface` → `color = colors.onSurface`.
  - KEPT: `:448` favorite icon `tint = colors.primary`.
  - Commit: `git add app/src/main/java/tv/own/owntv/features/search/SearchScreen.kt && git commit -m "Search: neutral chips and subtitle; ring-only result focus"`
- [ ] **Step 3: Gate** after each commit: `./gradlew :app:compileStandardDebugKotlin lintStandardDebug` green; `git status --porcelain -- app/src/main/res` empty.

### Task 2: EPG + Downloads color edits

**Files:**
- Modify: `app/src/main/java/tv/own/owntv/features/epg/EpgScreen.kt` (:385, :392, :769)
- Modify: `app/src/main/java/tv/own/owntv/features/epg/GuideCore.kt` (:238)
- Modify: `app/src/main/java/tv/own/owntv/features/downloads/DownloadsScreen.kt` (:230, :297)

- [ ] **Step 1: EPG.** `EpgScreen.kt:769`: `color = if (focused) colors.primary else colors.onSurface` → `color = colors.onSurface` (drop the ternary; rename `focused` to `_` only if genuinely unused in the lambda). `:385` stats text and `:392` match-summary text: `colors.primary` → `colors.onSurfaceVariant`. `GuideCore.kt:238` channel-name label: `colors.primary` → `colors.onSurfaceVariant` (keep `FontWeight.SemiBold` — labelMedium uppercase channel tag relies on it; it is a weight choice, not an accent). DO NOT touch `GuideCore.kt:135` catchup glyph, the `colors.favorite` now-line, or any progress fill.
- [ ] **Step 2: Downloads.** `:230` storage text: `color = colors.primary, fontWeight = FontWeight.SemiBold` → `color = colors.onSurfaceVariant`, drop the weight override. `:297` COMPLETED status text: same change. DO NOT touch the progress-fill `background(colors.primary)` at :234/:304.
- [ ] **Step 3: Gate + commit.** `git add app/src/main/java/tv/own/owntv/features/epg/EpgScreen.kt app/src/main/java/tv/own/owntv/features/epg/GuideCore.kt app/src/main/java/tv/own/owntv/features/downloads/DownloadsScreen.kt && git commit -m "EPG/Downloads: neutral informational text; ring-only guide focus"`

### Task 3: HomeGuideSlice + TopBar focus-fill leftovers

**Files:**
- Modify: `app/src/main/java/tv/own/owntv/features/home/HomeGuideSlice.kt` (:248-249, :387-388, :455-463)
- Modify: `app/src/main/java/tv/own/owntv/features/shell/components/TopBar.kt` (:201 + any sibling `focusedContainerColor = colors.primary`)

- [ ] **Step 1: HomeGuideSlice.** `:248-249`: the `color = if (focused) colors.primary.copy(alpha = 0.42f) else ...` ternary → keep only the else arm (read the else arm in situ and use it unconditionally). `:387-388`: same for the `0.82f` variant. `:455-463` tile: `background(if (focused) colors.primaryContainer else colors.surfaceContainerLowest)` → `background(colors.surfaceContainerLowest)`; text `color = if (focused) colors.onPrimaryContainer else colors.onSurfaceVariant` → `color = colors.onSurfaceVariant`. DO NOT touch the progress fill at :543 or the `:370` alpha-on-focus of `onSurfaceVariant` (neutral-on-neutral emphasis, not accent — leave as-is).
- [ ] **Step 2: TopBar.** Grep the file for `focusedContainerColor = colors.primary`. Delete each such override on interactive pills (:201 region and siblings) so `FocusableSurface` falls back to its default focused container; if the pill's content color has a matching `if (focused) onPrimary...` arm, neutralize it to the unfocused color. The STATIC `SectionChip` accent tint (~:139-144, display-only, no focus) stays. Record each pill changed.
- [ ] **Step 3: Gate + commit.** `git add app/src/main/java/tv/own/owntv/features/home/HomeGuideSlice.kt app/src/main/java/tv/own/owntv/features/shell/components/TopBar.kt && git commit -m "Home guide slice + top bar: ring-only focus, no accent fill swaps"`

### Task 4: PlayerHud follow-ups

**Files:**
- Modify: `app/src/main/java/tv/own/owntv/player/PlayerHud.kt` (error-scrim block ~:650-670; live bubble comment ~:1290-1300; NextEpisodeCard ~:1119; dialog values ~:1418/:1498/:1529; TopBar scrim application site)

- [ ] **Step 1: Error-scrim width.** In the center error/status block, move width control to the scrimmed `Column`: add `widthIn(max = 560.dp)` to the column carrying `hudTextScrim()`, and delete the `fillMaxWidth(0.8f)` modifiers from the two detail `Text` children so the scrim hugs its text up to the cap.
- [ ] **Step 2: Bubble comment.** At the live bubble (~:1297), replace/extend the comment to note both placements: VOD lifts the bubble with `offset(y = -32.dp)` because its parent is height-constrained; live pads with `padding(bottom = 30.dp)` inside an unconstrained parent — behavior differs by ~2dp historically and is deliberately preserved. Comment only, zero code change.
- [ ] **Step 3: NextEpisodeCard + dialog values.** `:1119` "Next episode in Ns" label: `colors.primary` → `colors.onSurface`. `:1418`, `:1498`, `:1529` (A/V-sync delay, volume %, subtitle-delay readouts): `colors.primary` → `colors.onSurface` with a one-line comment `// Neutral value readout — phase-2 stepper contract.` at the first site only.
- [ ] **Step 4: Empty-metadata guard.** At the TopBar title/meta stack where `hudTextScrim()` is applied: wrap so the scrim modifier (and the stack) is skipped when `displayTitle.isEmpty()` AND the chip row has no content (verify the real variable names in situ; the guard must not change layout when metadata exists).
- [ ] **Step 5: Gate + commit.** `git add app/src/main/java/tv/own/owntv/player/PlayerHud.kt && git commit -m "PlayerHud: bounded error scrim, neutral readouts, empty-meta guard"`

### Task 5: CTA enforcement sweep (PRIMARY-at-rest = single default action)

**Files:**
- Modify: `app/src/main/java/tv/own/owntv/ui/components/OwnTVButton.kt` (doc comment)
- Modify: any file where a demotion is required (discovered during the sweep; expected mostly dialogs in `features/` and `ui/components/`)

- [ ] **Step 1: Ruling comment.** On `enum class OwnTVButtonStyle`, add: `/** PRIMARY (solid accent at rest) marks the SINGLE default action of a dialog/action row; every other action is SECONDARY. (Design ruling, phase 5.) */`
- [ ] **Step 2: Sweep.** List all `OwnTVButton(` call sites (`grep -rn "OwnTVButton(" app/src/main/java`). For each visually-contiguous action group (a dialog's button row, an inline action row), count buttons that resolve to PRIMARY (explicit or by default). Where a group has ≥2 PRIMARYs at rest, keep PRIMARY on the affirmative/default action (the one a remote-holding user most likely wants — e.g. "Resume" over "Start over", "Done" over "Reset", "Close" in information-only dialogs) and add `style = OwnTVButtonStyle.SECONDARY` to the rest. Single-button surfaces and already-mixed groups are untouched. Record EVERY demotion (file:line, group, which stayed PRIMARY, why) in the report — this list is the reviewable artifact.
- [ ] **Step 3: Gate + commit.** `git add <exact files changed> && git commit -m "Enforce single-default-action rule for PRIMARY buttons"`

### Task 6: Orphaned-strings prune

**Files:**
- Modify: `app/src/main/res/values/strings.xml` + every `app/src/main/res/values-*/strings.xml` (deletions only)

- [ ] **Step 1: Candidate list.** Run `./gradlew :app:lintStandardDebug` and read the `UnusedResources` entries for `strings.xml` from `app/build/reports/lint-results-standardDebug.html` (or run lint with `--check UnusedResources` if faster). Collect the candidate keys.
- [ ] **Step 2: Verify each.** For every candidate key, `grep -rn "R.string.<key>\|@string/<key>" app/src worker/ 2>/dev/null` — delete ONLY keys with zero hits outside the res files themselves. Any hit (including reflection-ish dynamic lookups — grep the bare key name too) → keep, record as kept-with-reason.
- [ ] **Step 3: Delete mirrored.** Remove each confirmed-dead key from `values/strings.xml` AND from every `values-*/strings.xml` that carries it (`grep -l "name=\"<key>\"" app/src/main/res/values*/strings.xml`). Zero additions, zero edits to surviving strings.
- [ ] **Step 4: Gate + commit.** Compile + lint green (UnusedResources count drops; PluralsCandidate still clean). `git add app/src/main/res && git commit -m "Prune orphaned string resources"` (res-only commit is expected here; verify `git status --porcelain` shows ONLY res deletions staged).

### Task 7: Verification sweep + finish

**Files:** none (fix-forward only).

- [ ] **Step 1: Suite.** `./gradlew testStandardDebugUnitTest lintStandardDebug` → green.
- [ ] **Step 2: On-device (controller).** Build + install `standard` on `emulator-5554` (Audit profile). Screenshot + judge: (a) Search idle — jump-to chips neutral at rest, ring-only on focus; result rows (type a query via D-pad keyboard if feasible, else code-verify) don't recolor; (b) EPG screen — stats line neutral; guide grid (no feed → whatever renders) label focus = ring only; (c) Downloads — storage line neutral, progress fill still accent; (d) Home guide slice — focused tiles/rows show ring only, no accent alphas; (e) TopBar pills — focus = ring, no accent fill; (f) a multi-button dialog affected by Task 5 (e.g. the Resume/Start-over dialog) — one PRIMARY, rest SECONDARY; (g) player quick-pass — NextEpisodeCard/dialog values if reachable, else code-verify.
- [ ] **Step 3: Fix findings, re-verify, commit each fix.** When clean: final whole-branch review (most capable model), ONE fix wave if findings, then `superpowers:finishing-a-development-branch`.

## Self-Review

1. **Spec coverage:** ruling→Task 5, §2→Task 1, §3→Task 2, §4→Task 2, §5→Task 3, §6→Task 4, §7→Task 6, verification→Task 7. Contract table rows all mapped; KEPT list embedded in Global Constraints. No gaps.
2. **Placeholder scan:** clean — every color edit names exact before/after values; extraction and sweep steps carry bounded judgment rules with report-recording; no "TBD"/"similar to".
3. **Type consistency:** `ResultRow` is file-private to SearchScreen.kt (named once, no cross-task consumer); `OwnTVButtonStyle.SECONDARY` matches the real enum; no new shared interfaces between tasks.
